# Архитектура системы

## Общая структура

```
┌─────────────────────────────────────────────────┐
│              Telegram (внешний мир)              │
└──────────────────┬──────────────────────────────┘
                   │ SOCKS5 прокси
                   ▼
┌─────────────────────────────────────────────────┐
│          MacBook Air  (Бишоп 🐾)                │
│  IP: 192.168.50.6 / 100.83.112.11               │
│  Роль: SOCKS5 прокси + страховочный агент       │
│  OpenClaw: порт 18789                            │
└──────────────────┬──────────────────────────────┘
                   │ Tailscale / локальная сеть
                   ▼
┌─────────────────────────────────────────────────┐
│        Новый сервер (evgeniy-1)                 │
│  Локальный IP: 192.168.50.15                     │
│  Tailscale: 100.114.32.123                       │
│  CPU: Intel Core i9-14900 (32 ядра)              │
│  RAM: 62 GB                                      │
│  Диск: 1.83 TB NVMe (Samsung)                    │
│  ОС: Ubuntu 24.04.4 LTS                          │
│  GPU (будет): Tesla V100 32GB                   │
└──────────────────┬──────────────────────────────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
   ┌────────┐ ┌────────┐ ┌────────┐
   │ Боты   │ │ Сеть   │ │ Память │
   │ 8 шт   │ │ Docker │ │ 5 ур.  │
   └────────┘ └────────┘ └────────┘
EOF

cat > /data/lightrag/wiki/Models.md << "ARCH"
# Модели (Ollama)

## Текущие (CPU)
| Модель | Размер | Назначение |
|--------|--------|------------|
| qwen2.5:1.5b | 986 MB | Основная модель для ботов, entity extraction, self-learning |
| bge-m3 | 1.2 GB | Эмбеддинги для LightRAG и Memory Service |

## Скачаны, в ожидании GPU
| Модель | Размер | Заметка |
|--------|--------|---------|
| qwen3:8b | 5.2 GB | Лежит, ждёт Tesla — включится как основная |
| deepseek-r1:14b | 9.0 GB | Режим reasoning, на GPU полетит |
| deepseek-r1:32b | ~20 GB | Качается для Tesla, Q4_K_M |

## Планируются
- Qwen3.5:4b — лёгкая для LoRA fine-tuning
- DeepSeek V3 — для глубокого анализа диалогов
